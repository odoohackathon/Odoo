import 'package:flutter/material.dart';
import 'package:oddo/res/utils/utils.dart';

class CheckCrimesScreen extends StatelessWidget {
  const CheckCrimesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: primaryTextAppBar(title: "Check Crimes"),
    );
  }
}
